apply from: "/${rootDir}/config/conventions/android-common.gradle"

android {
    compileSdk Versions.COMPILE_SDK
    namespace 'com.famy.us.domain'
}

dependencies {
    implementation project(':core')
    compileOnly libs.koin.core
}
