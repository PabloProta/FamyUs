package com.famy.us.authentication.state

/**
 * Enum class that represent the authentication state.
 */
internal enum class AuthenticationState {
    USER_LOGGED,
    AUTHENTICATION_FAIL,
    IDLE,
}
